import React from "react";

const SettingsPage = () => {
    // we can add: dark mode, reindex library, reset ratings, etc.
    return (
        <div>
            <h1>Settings</h1>
            <p>Customize your settings here.</p>
        </div>
    );
};

export default SettingsPage;